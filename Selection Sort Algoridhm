// Swap the smallest element with the first one.
then move onto the second elemnet and swap it with smallest again.
  for i - 0 to A.length-2
    minIndex = i;
     for j=i+1 to A.length-1
       if (data[j] < data[minIndex])
         minIndex = j;
    tmp = data[minIndex]
    data[minIndex] = data[i]
    data[i] = tmp;
